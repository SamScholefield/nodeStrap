extends layout

block content

  #memberlist.jumbotron
    .container
      h1 registered members
      ul
        each user, i in memberlist
          li
            a(href="mailto:#{user.email}")= user.firstName
      p
        a.btn.btn-default.btn-primary(href='/', role='button') Home »